import React from 'react';
import PropTypes from 'prop-types';

const UserRow = props => {
    return (
        <div>
            
        </div>
    );
};

UserRow.propTypes = {
    user: PropTypes.object
};

export default UserRow;